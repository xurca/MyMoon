import React from 'react';
import Typography from '@material-ui/core/Typography';

const RideDesc = () => (
  <Typography variant='subtitle1'>
    ქუთაისი
  </Typography>
);

export default RideDesc;
